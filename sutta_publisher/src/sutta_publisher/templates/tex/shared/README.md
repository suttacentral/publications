## Shared TEX templates guide

An environmental variable `LATEX_TEMPLATES_MAPPING` responsible for the mapping is included in `.env_public` file.

### Defining a shared template:


```python
LATEX_TEMPLATES_MAPPING = '{
    "<name>": "<file_name>.tex",
}'
```
